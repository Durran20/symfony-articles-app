{% extends "base.html.twig" %}

{# Now if we want a title #}
{% block title %}New Articles{% endblock %}


{# Now if we want the body #}
{% block body %}
<div class="w-full max-w-xs">
    <div class="bg-gray-200 shadow-md rounded px-8 pt-6 pb-8 mb-4">
        {{ form_start(form) }}
        {{ form_widget(form) }}
        {{ form_end(form) }}
    </div>
</div>
{% endblock %}